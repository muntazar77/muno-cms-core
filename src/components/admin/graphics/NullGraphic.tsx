// Returns nothing — used to suppress Payload's default Logo and Icon in the header.
// Registered via payload.config.ts > admin.components.graphics.Logo and .Icon
export default function NullGraphic(): null {
  return null
}
